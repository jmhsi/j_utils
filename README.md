a collection of python modules of functions that justin tends to reuse over and over. Most interesting things are probably in munging.py

In same dir as this README:
conda activate {environment_name}
pip install -e .
